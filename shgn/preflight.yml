---
- name: preflight
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: install required packages
      apt:
        pkg:
         - cephadm 
         - htop
         - screen
         - python3
         - python3-pip 
         - python3-systemd
         - fail2ban
         - git 
         - sshpass 
         - lvm2 
         - curl
         - ceph
           #- ansible
        update_cache: true
        state: latest
      ignore_errors: true
